name: Billions Human CTA

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Generate verification
        run: |
          echo "Human CTA verification started"
          echo "Go to Billions dashboard to bind agent"
